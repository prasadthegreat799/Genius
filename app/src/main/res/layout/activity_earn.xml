<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EarnActivity">



    <include
        android:id="@+id/questionlayout"
        android:layout_marginTop="10dp"
       android:layout_centerHorizontal="true"
        layout="@layout/earnlayout"
        android:layout_height="wrap_content"
        android:layout_width="match_parent" />

    <Button
        android:id="@+id/instructionbtn"
        android:text="Click Me For More Infomaion"
        style="@style/loginButton"
        android:layout_margin="15dp"
        android:layout_below="@+id/questionlayout"
        android:layout_centerHorizontal="true"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        />

    <Button
        android:id="@+id/contactusbtn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        style="@style/loginButton"
        android:text="Contactus"
        android:layout_marginTop="15dp"
        android:layout_below="@id/instructionbtn"
        android:layout_centerHorizontal="true"
        />

<include
    android:id="@+id/myearnadview"
    layout="@layout/adviewlayout"
    />

</RelativeLayout>